M6 -- does `flag_astrom_quiet` earn its column?  (2026-08-21)
==========================================================================
verdict store: 588 records; CONFIRMED[epoch_vet_harness/orbit_reality:380]  SPURIOUS[epoch_vet_harness/orbit_reality:208]
queue: 981 rows, flag_astrom_quiet marks 255 (26.0 %)

POPULATIONS -------------------------------------------------------------
  all verdicted EB26 targets (what M5 measured on): 588  (380 conf / 208 spur)
  verdicted AND in the day-one queue (what the flag actually operates on): 0  (0 conf / 0 spur)
  the frozen screen removes 588 of the verdicted targets before the flag is ever computed.
  -- a discriminator measured on a population it will never see is not
     evidence about the flag.  That is why this test exists.

  reconciliation with M5's published in-list counts:
    main bin only (M5's population): 0 verdicted (0 conf / 0 spur), flagged 0, of which spurious 0 -- M5 reported 46 / 2 / 0-of-7 and this reproduces it.
    + retrieval bin (Pr >= 0.999), which the flag ALSO marks: 0 more verdicted rows.
    = 0 in-list verdicted rows in total. This test uses the whole queue,
      because the whole queue is what the flag is computed on.

1. CONTINUOUS TEST, all-65 (M5's population) vs in-list (the flag's) ---
  C5 astrometric_gof_al (the frozen flag's metric) [all-65]: n 0/0 -- NOT TESTABLE
  C5 astrometric_gof_al (the frozen flag's metric) [in-list]: n 0/0 -- NOT TESTABLE
  C1 ruwe (the runner-up M5 deliberately did not freeze) [all-65]: n 0/0 -- NOT TESTABLE
  C1 ruwe (the runner-up M5 deliberately did not freeze) [in-list]: n 0/0 -- NOT TESTABLE

2. THRESHOLDED TEST -- the flag itself, on the in-list population ------
  flagged CONFIRMED 0/0 = 0.000 (95 % Wilson nan-nan)
  flagged SPURIOUS  0/0 = 0.000 (95 % Wilson nan-nan)
  Fisher two-sided p = 1.0000, odds ratio nan
  power: against a confirmed marking-rate of 0.000 at n = 0/0, the smallest SPURIOUS marking-rate detectable at 80 % power is NOT REACHABLE at any rate <= 1.0
  -- with 0 in-list spurious rows, the flag would have to mark essentially every one of them before this test could notice.
  M5's '0 of 7' is therefore NOT evidence that the flag fails: at a 0.000
     marking rate, the EXPECTED catch among 0 spurious rows is 0.00.  Observing 0 is what both a working flag and a
     dead one predict.  The in-list test has no power, full stop.

3. WHAT DECEMBER HAS TO HARVEST TO SETTLE IT -----------------------------
  (in-list verdicts; today's in-list confirmed:spurious ratio 0:0 held
   fixed, 80 % power at alpha = 0.05, two-sided MWU)

  The day-one queue is 981 rows.  The harness adjudicates all of them,
  so the in-list verdict count goes from 0 to O(981) in one pass -- which is more than enough for
  every row above.  This is the whole argument for M6 existing.

==========================================================================
DECISION ----------------------------------------------------------------
  -> CARRY
  CANNOT DECIDE UNTIL DECEMBER'S VERDICTS EXIST, and the reason is measured,
  not rhetorical: the population the flag operates on holds 0 spurious
  rows, the in-list continuous test cannot see anything short of the AUC
  printed above, and the thresholded test cannot see anything at all.  The
  flag stays exactly as config v4 froze it -- tiebreaker only, both caveats
  attached, never a cut and never quoted beside `significance` -- and the
  test that settles it is pre-registered in this script's docstring.
  Removing it today would be as unevidenced as promoting it.
==========================================================================

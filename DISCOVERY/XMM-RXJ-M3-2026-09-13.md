# RXJ M3: one bounded summary-only plain TAR

Prospective, no request yet. Adopt the complete frozen
[M2 next-step decision](XMM-RXJ-M2-NEXT-2026-09-13.md). M2 remains STOP_NOT_HTML;
M1's wildcard/package names are not used as addresses. Exact one GET:

```text
https://nxsa.esac.esa.int/nxsa-sl/servlet/data-action-aio?obsno=0851180501&level=PPS&name=SUMMAR&extension=HTM
```

Fresh anonymous session, no environment auth/proxies, cookies, redirects, Range,
HEAD, retries, follow-ups or linked resources. Identity encoding, 5/15 second
socket timeouts, audited 30-second worker process-tree deadline for transfer
and archive inspection. Parent and explicitly invoked replay each add one
bounded local archive-validation pass; no request. No scientific product opens.

Require HTTP200 and exact URL, absent/identity encoding, application/x-tar or
application/tar. Optional Content-Length: positive decimal ≤1,048,576, exact
complete-byte equality. Raw cap1MiB plus one unretained overflow byte. EOF has
its own exclusive receipt. Safe six-name header profile: one ASCII value/name,
≤1,024 characters; duplicate framing or controls STOP before unsafe persistence.
HTTP parser≤65,536 bytes/line and100 headers. Optional outer disposition uses
the bounded M2 parser, retaining only status/kind/accepted-name hash. Its ABSENT,
REJECTED or other status is non-authoritative and never a package identity gate.
No raw disposition or arbitrary response URL/error/output text is persisted.

Raw bytes go to fixed locally ignored summary.tar. Hash before inspection.
The pure hash-pinned xmm_summary_tar helper accepts at most16 physical headers,
one positive regular summary member≤262,144 bytes and zero-sized directories.
Checksums use stdlib TarInfo.frombuf, never extraction or automatic extension
processing. Reject PAX/GNU longname/longlink/sparse/special/link records explicitly;
support ordinary USTAR prefix and compatible plain legacy/GNU headers without
a magic-brand gate. Required two zero terminator blocks and zero trailing blocks
reject truncation/concatenation. Per-member alignment padding must be in bounds
but need not be zero. No other member payload is copied or interpreted.

Relative ASCII paths≤512 bytes and≤8 substantive components; harmless dot/empty
components normalize, root directory allowed. Reject parent traversal, absolute,
drive/colon, backslash, controls/nonASCII, percent and unsafe punctuation. Duplicate
normalized paths STOP. No required folder prefix or order. Sole basename follows
the adopted observation/SUMMAR/HTM PPS field positions without selecting an
unobserved instrument/exposure. Directory owner/group text is irrelevant and
never serialized. Parsed name hashes refer to TarInfo's resolved name; complete
header hashes preserve raw representation. Standard-library legacy NUL slash-name
directory interpretation is admitted only with zero size, like other directories.

Only after the entire archive validates, copy the admitted private HTML byte
slice to fixed ignored summary.html, never any archive-provided path. Public
inventory contains ordinal, name/canonical-name/header hashes, size, type, offset
and admitted file-content hash; no names, owner fields, contacts or coordinates.
No HTML parser, footer, identity, exposure or scientific conclusion here. Strongest
label SUMMARY_MEMBER_RETAINED_UNADJUDICATED; error pages remain possible.

Fresh verified-buffer M2 composition changes exactly three quoted raw-body
filename literals from summary.html to summary.tar before its existing reviewed
M0 adapter is invoked. No other inherited source text changes. Explicitly replace
collection/MIME classification and private archive interpretation with M3 functions.
Existing one-shot markers, bounded safe receipts, parent/replay closure, raw return
code and unknown interrupted work remain. Worker alone writes private HTML;
parent/replay compare its exact byte count/hash against a new archive pass and
never create or repair it. Each JSON≤64KiB, aggregate≤1MiB,64KiB terminal reserve.
Raw and private HTML caps are separate; Python metadata overhead is not an OS quota.

Bind frozen M2 source/tests/protocol/STOP/HTTP, adopted LF decision, pure helper/tests,
all inherited M1/M0/deadline lineage, new source/tests/protocol, privacy/attributes
and owner runtime. Independent synthetic helper and full mocked worker-parent-replay
tests, privacy/partial/overflow/unsupported archives and tamper tests precede exact
freeze. No live request until root execution. Unsupported package STOP preserves
raw bytes for a possible separately reviewed offline decision, never another GET.

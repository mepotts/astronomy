# RXJ M6: fresh three-product transfer with a longer finite worker

Prospective, unexecuted. Adopt the complete parent-approved
[M5 next decision](XMM-RXJ-M5-NEXT.md), SHA256
94cdd8bd9272521cbdc943845604f0e226acab29641b32e042a91f03ae4490c6.
This contract incorporates all unchanged transport, artifact, resource, privacy,
identity and scientific limitations of the frozen
[M5 protocol](XMM-RXJ-M5-2026-09-13.md), SHA256
7273718a789146accd9ecd5c51b9b36cea698a25234b093fc419216cb16a425e.
M5's timeout, unverified 27 MiB PN partial and every earlier STOP stay immutable.
M6 neither opens nor appends to that partial, and does not invoke M5's CLI/replay.

## Exact requests and unchanged acceptance

At most one complete GET each, in PN/M1/M2 fail-stop order:

1. `https://nxsa.esac.esa.int/nxsa-sl/servlet/data-action-aio?obsno=0851180501&level=PPS&instname=PN&expflag=S&expno=001&name=PIEVLI&datasubsetno=0&sourceno=000&extension=FTZ`
2. `https://nxsa.esac.esa.int/nxsa-sl/servlet/data-action-aio?obsno=0851180501&level=PPS&instname=M1&expflag=S&expno=002&name=MIEVLI&datasubsetno=0&sourceno=000&extension=FTZ`
3. `https://nxsa.esac.esa.int/nxsa-sl/servlet/data-action-aio?obsno=0851180501&level=PPS&instname=M2&expflag=S&expno=003&name=MIEVLI&datasubsetno=0&sourceno=000&extension=FTZ`

Require exact named dispositions P0851180501PNS001PIEVLI0000.FTZ,
P0851180501M1S002MIEVLI0000.FTZ and P0851180501M2S003MIEVLI0000.FTZ.
Fresh anonymous sessions, no environment credentials, cookies, HEAD, Range,
If-Range, redirects, retries, broader selectors or packages. Same HTTP200,
identity encoding, safe bounded header validation, binary MIME allowlist and
optional Content-Length/actual EOF requirements. Same gzip CRC/EOF or raw FITS
magic, exact private fixed paths and immutable partial receipts; no array decode.
First failure leaves every later slot NOT_ATTEMPTED. No automatic second attempt.

## Only adopted execution changes

One tree-aware worker has a 7,200-second hard deadline; inherited worker code uses
a 7,140-second cooperative checkpoint deadline. The remaining 60 seconds are a
reporting margin, not a renewed transfer. Parent header/hash assessment and one
explicit root replay each have an independent 300-second cooperative deadline,
not another worker tree allowance. Connect/read socket timeouts remain 5/15 seconds.
No deadline or cap is changed from actual M6 outcomes.

Network read requests are at most 64 KiB (including a remaining-cap+1 request),
while local copy/expansion/hash chunks remain 1 MiB. The adapter temporarily changes
the isolated module's CHUNK only during its single-threaded network copy and
restores it in finally. This improves checkpoint granularity, not proven speed.
Missing worker completion after typed return code124 is STOP_WORKER_HARD_DEADLINE;
the raw124, partial artifacts and unattempted slots remain visible. Completed
worker receipts are still independently assessed, and124 cannot promote success.
If terminal parent serialization also exceeds the monitored memory cap, its
STOP_PEAK_MEMORY may take primary precedence; replay accepts that combination
only with an explicit above-cap integer parent peak, STOP/unassessed status and
the unchanged raw124/missing-worker evidence. Header work is not invented.

All other limits remain: raw256 MiB/file and512 MiB/batch; expanded1 GiB/file and
2 GiB/batch; real monitored Windows peak500,000,000 bytes; fresh free disk4 GiB;
aggregate JSON10 MiB, private header/public identity files2 MiB each, terminal
reserve256 KiB and compact failed slot64 KiB. These are finite acceptance limits,
not an allocation quota or a prediction of transfer completion. Full header cards
and products are ignored locally before any run; public derivatives remain safe.

## Minimal isolated composition and proof

Load the exact frozen M5 source from one verified byte buffer into a fresh module,
which itself loads a fresh verified C1 primitive module. Rebind HERE, SOURCE,
PROTOCOL, __file__, C.HERE and worker deadlines to M6 before any stage execution.
No historical M5 run, binding comparison against its run-start, product access or
replay occurs. The inherited fixed three-slot plan is reused only in M6 state.

Retain original binding validation and all dependencies, adding frozen M5 source,
tests, protocol, public outcome/HTTP receipt and the adopted next decision. Bind
current M6 source/tests/protocol/privacy files and runtime. The binding's separate
hard/cooperative/parent/replay times and network/local sizes are stable even during
temporary assess/copy overrides. Worker dispatch always uses the M6 source path.
Original worker/ledger/expansion/header/identity/replay mechanics execute only in
this isolated context; the small M6 parent dispatcher chooses7200 and classifies
missing-worker124. No old source string substitutions or old private file reads.

Inherited parent/replay header verification performs no decompression, rechecks
hashes/layout/identity and retains compact per-slot partial-pass flags. Artifact-
only replay is not completed header verification. No FITS checksum verification,
science values, photon counts, clean controls, pn-plus-MOS scientific eligibility
or discovery are established. Strongest success remains
RXJ_EVENT_PRODUCTS_RETAINED_HEADERS_ONLY.

Before freeze: run all16 frozen M5 synthetic regressions against the isolated M6
module, plus focused fresh-path, stable binding, separate deadlines, smaller
network reads/cap+1 and missing-worker124 partial/replay checks. A representative
synthetic physical-file fit may confirm mechanics/resources; it cannot prove a
two-hour archive transfer will complete. Parent exact-byte freeze is required
before any request; no request is authorized by this prospective file alone.

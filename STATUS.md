# STATUS — live research dashboard

*Current portfolio snapshot: 2026-09-05. Per-project `STATUS.md`, milestone documents,
operating guides, and committed artifacts are the detailed record. The older wave tables
below are retained as a dated audit trail, not as the current queue.*

**House gates (always on):** nothing submitted anywhere (MPC, TNS, journals, emails).
Matthew authorized tested commits, pushes and merges for the current closeout; that does
not authorize scientific publication or amend explicitly frozen protocols. Agents verify
before building — sweep-reported facts are treated as claims until re-checked with this
repo's own tooling. Every externally-sourced number carries its source or the mark
UNSOURCED (exosat-rv LESSONS §5b, repo-wide law).

## Current portfolio snapshot

The aggregate, identity-free execution record and next gates are in
[`DISCOVERY/CLOSEOUT-2026-09-05.md`](DISCOVERY/CLOSEOUT-2026-09-05.md), with
[publication decisions](DISCOVERY/PUBLICATION-DECISIONS-2026-09-05.md) and the
[new-work comparison](DISCOVERY/NEW-WORK-2026-09-05.md). September 2's campaign remains history.

| Front | Current state | Next gate |
|---|---|---|
| [`itf-linker/`](itf-linker/) | **Daily archive repaired and caught up.** September 5 snapshot and release key set published; watch green, 20 ready / 6 held unchanged. August 6 metadata repaired against unchanged scientific bytes. M14 stays stopped; no new queue. | Verify the first unattended cycle, then monitor meaningful freshness/count changes. New attribution work requires a separately frozen provenance-valid protocol. |
| [`tns-miner/`](tns-miner/) | **PARKED after 15 bounded recovery probes.** Required Fink class and six ALeRCE REST/TAP alternatives remain unavailable/incomplete despite healthy controls. No fresh campaign or candidate count. | Prove a complete enumerator, then a fresh three-day window and post-window TNS snapshot; never promote the stopped bundle. |
| [`dyson-revet/`](dyson-revet/) | **M7 closed; E WAIT_RELEASE.** D control 7/7 and outcome map unchanged; 39 exclusive MAST observations still release September 9. New bounded release guard fails closed. | Scheduled September 9 follow-up must execute the frozen experiment after public access; M7 validity checks and human publication gates remain. |
| [`gaia-dr4/`](gaia-dr4/) | **M9 ready for planned December 2 release.** Successful September 2 rehearsal retained; concrete proposed label/power rulings prepared, not applied. | Matthew's explicit frozen-rule ruling; public date/schema and actual release-day execution. No security exclusions/accounts changed. |
| [`chime-frb-periodicity/`](chime-frb-periodicity/) | **PARKED.** Targeted recheck found no authenticated time-resolved observing/sensitivity product; the published periodic control remains recovered. No unknown scan. | Restart only with the exact citable time series and complete M1 protocol. |
| [`dasch-pilot/`](dasch-pilot/) | **Original M0 closed: stop unconditional faint-field annotation.** R Cnc passes coverage; V404 Cyg has only 5 clean detections. Original image selection fails; separately exploratory depth-matched plates qualitatively recover its published 1938 outburst. | Only a possible externally vetted bright stable-star calibration study. APASS class/v_flag does not prove stability; no blind search earned. |
| [`spherex-pilot/`](spherex-pilot/) | **Broad scan killed; narrow path blocked at privacy gate.** Only 1/223 fitted warm tails clears the conservative 4.8-micron floor; forced spectrophotometry is already an official IRSA capability. Zero private coordinates were sent. | If explicitly approved, query coverage for the exact six-row private manifest; otherwise leave the paired warm-tail test blocked. |
| [`pta-mpta/`](pta-mpta/) | **Analysis closed; local review bundle built and extracted checks pass.** Three draft audits pass; raw chains and some reproduction dependencies remain outside the compact package. | Choose primary paper/overlapping notes, actual author/venue/archive/DOI and exact submission. |
| [`erosita-dr2/`](erosita-dr2/) | **Corrected census review package ready.** Prior-art, contamination, physical-rate, classifier and artifact-percentage claims narrowed. The 148/107/6 classification survives, not 107 confirmed discoveries. | Human decision on the modest census note; exact archive/author/venue/submission. Private follow-up remains separate. |
| [`ccor-pilot/`](ccor-pilot/) | **Finite pilot closed: STOP_METADATA_QUALITY.** Four public FITS files lack the frozen required ISVIABLE flag. Additional WCS rotation/display-provenance barriers remain. No real pixels scored. | New documented quality/orientation protocol and defensible control required before any recovery or unknown-source search. |
| [`adql-copilot/`](adql-copilot/) | Packaged tool; 46-test suite and JOSS draft. | Publication is separate from repository integration. |
| [`seti-ellipsoid-broker/`](seti-ellipsoid-broker/) | Packaged tool; 84 tests plus 2 default skips and an RNAAS draft. | Publication is separate from repository integration. |
| [`pta-explainer/`](pta-explainer/) | Deployed explainer; 64 tests and production build. | Site publication/deployment remains separately controlled. |

## Current safety boundary

Merging verified repository changes may make default-branch CI and schedules available;
it does **not** authorize any DOI mint, journal upload, MPC/TNS payload, email, account
creation, unrelated scheduler mutation, or public announcement. The current closeout's
ITF catch-up and dated follow-ups are authorized; outward scientific actions remain human
decisions. See [PUBLISHING.md](PUBLISHING.md) for the publication queue and each project's
operating guide for registry-facing work.

## Historical dashboard snapshot — wave 8 (launched 2026-08-24)

| Front | Folder | Scope |
|---|---|---|
| dyson-revet **M7 ✓ — FRONT CLOSED** | [`dyson-revet/`](dyson-revet/) | **The named fix worked, the acceptance test passes 6 of 6 through byte-identical code, and the answer is still that the published redshift cannot be independently checked — which is now a much stronger statement, because the deblend is no longer a candidate explanation.** An **empirical, wing-carrying PSF built from the cubes already on disk** (donor rule: whichever component dominates a sub-band *is* the PSF there — 10 of 12 qualify) takes M6's six flux ratios from **0.96 / 0.69✗ / 0.77 / 0.84 / 1.52✗ / 0.75** to **1.09 / 0.78 / 0.97 / 1.02 / 0.76 / 0.98**. **The test was not weakened anywhere**: the extraction was written to the filename and schema the M6 grader already reads, so `m6_mrs_redshift.py` graded it **unmodified** — same ±30%, same reference fluxes, same six comparisons, PASS requires 6 of 6. **Both of M6's failures were on the sub-dominant component and both closed.** **The leakage is now a number**: injecting a companion of *known* flux fraction into the cubes at PA+90°, a wingless Gaussian **under-recovers a 1%-contrast companion by 42% (59% on resolved sub-bands), 7% at 5%, 1% at 30%**, while the empirical PSF is flat at **1.00** — M6's failure pattern reproduced against a known truth, with the circularity priced by a full 2×2. **And with the deblend validated the spectrum still cannot pin z:** the blind XCF peaks at z = 2.436 at **2.60× the scan rms, BELOW the 3.92× the star control reaches**; ±0.02 dex of continuum still moves it by **Δz ≈ 1.5**; **42.9% of the redshift grid matches z = 0.922** on the blind narrow-line scan and 0.922 is not among the best. *Consistent with a PAH-bearing obscured galaxy at z ≈ 0.9 — 7 of 8 features at ≥5σ, silicate trough at z = 0.833, M4's 441 K now 384 K — but the same procedure finds that much at 43% of all redshifts.* **Sharpest methodological finding, and it is a correction to ourselves: M6 PR-2's redshift criterion could never have decided this case.** The fitter searches ±0.5 FWHM, so a **narrow** line can only move by **Δz ≤ 0.0028** and satisfies the "±0.01 agreement" clause **vacuously at any redshift**, while **PAH 7.7 alone roams ±0.112** so one detected broad PAH forces it to fail — the criterion is decided by *which kind of feature* is detected, not by whether z is right. **Reported as a defect and NOT used to re-read the result.** **A transferable limit, and it applies to E:** the pair falls from **4.38 to 1.26 PSF FWHM** across the cubes and below ≈2 FWHM no deblend is determined by either method — the model-free symptom is an **unphysical negative flux**, reaching **86% of slices**. Restricting to determined sub-bands **moves neither verdict** (M6 still 4/6 digit-for-digit; M7 still 6/6, worst ratio **0.76 → 1.00**), so the pass is robust and the improvement is the PSF and not the restriction. **PR-1 made a prediction and LOST it** — the stitching offsets did *not* shrink (median |dev| 0.109 → 0.138), so M6's "the offsets are the limit" is only half right. **PR-1's declared star-scaled sensitivity FAILS 5/6** and its one failure (F1500W star 1.41) is diagnostic — the donor rule is doing real work. **Completeness is materially family-dependent and PR-2's pre-declared 5-point trigger fired at 19.75**: 100,800 new injections through the **unmodified** pipeline give in-grid |b|>30° recovery of **26.1%–44.4%** across six arms against the matched single-blackbody control's 43.7%, **monotonic in how far the SED is from a blackbody** (f_warm 0.7 is indistinguishable at +0.64; f_warm 0.3 is −17.6). **M6's 1000 K temperature wall is WITHDRAWN as a property of the screen** — a two-temperature shell recovers **30–37% at T_warm = 1000 K** because T/3 is back inside the grid, while optically-thin dust hits **0.0% already at 700 K (β=1) and 450 K (β=2)**: **every family has its own wall at its own temperature**. **The γ=0.05 blindness is family-dependent too** (1.8% blackbody vs **11.3%** optically-thin — six times less blind), though the γ ≤ 0.02 floor is absolute in every family. **The tabulated `t_ds`/`gamma` of any row are biased −36% to +48% if the SED is not a blackbody, and the RMSE gate does not catch it** (accepted non-blackbody objects sit at 3–5× the residual and still clear 0.2 mag). **Both PR-2 controls passed**: the new generator reduces analytically to M6's family (f_warm=1 exact at 0.0; β=0 at 8.03e-08 mag, **traced to a truncated Stefan–Boltzmann constant in the pre-existing `w1_selection.py`**, not the new code — tolerance moved to 1e-6 mag **with the reason on the record**), and the run-through arm lands at 43.7% vs M6's 45.8%, setting the run's own **~2-point Monte-Carlo floor**. **Catalogue v3 shipped** — 223 rows **byte-identical to v2 and asserted so**, completeness restated per family, README opening on *"there is no single completeness number for this catalogue"*; **v1 and v2 checksum-verified unmodified**. **Candidate E READY, nothing pre-empted**: outcome map byte-identical to the committed M5 (same SHA-256 as M6 recorded), chain reproduces M4 §5 on **7/7**, **0 PUBLIC of 39, release 2026-09-09**, nothing fetched. **M7 also found and fixed two of its own bugs** — an encircled energy > 1 from normalising over a negative wing, and a per-bin-vs-per-slice FWHM normalisation error — **both caught by internal checks, not by the headline test, which passed either way**. **[`FRONT-SUMMARY.md`](dyson-revet/FRONT-SUMMARY.md) is the front's stand-alone hand-over document**: what the re-screen established (funnel 4.20× → **1.117×**, every stage reproducible and parameter-free), what it could not (**the 12.7-point RMSE-gate residual**, the **1–2″ centroid blind spot with a measured 49.9° direction error**, the completeness walls), what the catalogue is and is not usable for, and the dated triggers. **M8: none needed.** The front is done pending **E on 2026-09-09**, **A on 2027-07-16**, and Matthew's three calls — the Ren+24 note, the I dossier, and **whether §2.3's negative gets written up** (gated: it is a negative about a published claim) |
| pta-mpta **M6 ✓ — CLOSING MILESTONE** | [`pta-mpta/`](pta-mpta/) | **Three documents, all DRAFT — NOT SUBMITTED, and the only blocker left on the paper is the DOI.** No chains: M6 is citations, one external sweep, a cold read, a new note and verification. **The prior-art re-sweep changed the positioning on two of four claims, and both are declared.** **(1) Claim (iv) is PARTIALLY SCOOPED — Larsen et al. 2025 (MNRAS 542, 3028) §4.1.4/Fig. 8 already publish a drop-one-pulsar analysis of a DIFFERENCE of two factorised-likelihood products** ("the overall discrepancy is sensitive to systematic errors in the individual pulsars"), and Reardon+23 already name the pulsars that "dominate the factorized likelihood". The paper's §6.2 and the new methods note now **lead with that credit**; what survives is narrower and says so. **Larsen does not cite the MPTA paper, so no citing-works sweep could ever have found it** — a citation graph is blind to parallel work that does not cite you, and that lesson is now in the paper. INSPIRE has **76** citers vs OpenAlex's 44; all 76 enumerated, nothing else. **(2) Claim (ii) REFRAMED: the missing prior is public, as uncited code.** Verified in-session: `github.com/MattTMiles/MPTAGW` (first author's repo, no README/licence/tag/DOI, cited nowhere) sets **`gamma_sw = Uniform(-4, 4)`** — **the exact range our blind registered variant chose**, and its `log10_A_sw = Uniform(-10, 1)` matches our declared prior too. "Undocumented" survives, "unreachable" does not, and every such phrasing was fixed; the coincidence *strengthens* the diagnosis. **Citations filled — the last UNSOURCED slots are gone**, each package in the form its own authors ask for (7 traps: e_e wants **no DOI**; PINT wants **two** papers; tempo2 wants I+II and **we never ran it**; parallel-bilby is **Vajpeyi** not Callister; `enterprise_warp` has no citation at all), versions inline. **The facility policy was read, not assumed: PTUSE requires a second acknowledgement "in addition to" SARAO's**, both now verbatim. **M1's "no license stated" is CORRECTED — the release is CC BY 4.0** in its DataCite metadata. **Cold read: ten changes**, including the title and *"We withdraw one array-level claim of our own"* now in the abstract (trimmed 333→**240** words; MNRAS's 250 verified live). **New methods note DRAFTED — NOT SUBMITTED** (`draft-rnaas-composition-jackknife.md`, RNAAS, 1,300 words, 42 numbers, 49 checks, falsification-tested): *a factorised product's credible interval understates its composition dependence, so a difference of two against a fixed threshold is not a significance test.* **Verification: paper 137 numbers / 119 checks / 0 fail; table note 29 / 22 / 0; methods note 42 / 49 / 0 — and the pass found FOUR real errors of ours**: a reference with the wrong author list, two growth-curve ranges that failed re-derivation, **32 content numbers the paper's own audit never covered** (105→137 rows), and a hard-coded fallback inside the audit script | **Mint the Zenodo DOI — still the single blocking item, and now the ONLY one** |
| pta-mpta **M5 ✓** (superseded by M6) | [`pta-mpta/`](pta-mpta/) | **The paper is drafted** (`draft-paper-mpta-noise-reproduction.md`, ~6,000 words, DRAFT — NOT SUBMITTED). Headline: *an independent reproduction of the MPTA noise table from public data agrees with 576 of its 588 values, every disagreement traces to a single undocumented prior, and that column is mostly not a measurement.* 105 numbers re-derived, 92-check verifier at 0 failures, ten-row corrections-to-ourselves section. Drafting itself found two accuracy fixes: their posteriors are **nested sampling**, so this reproduction crosses sampler families; and the prior ranges do exist machine-readably in their pipeline. **The census is the publishable number: of 26 published γ_SW rows only FIVE are measurements** — 5 more have an apparent constraint that is the prior edge, 15 were never constrained; **20 of 26 are not measurements**, and a reader can flag 18 from the printed table but **not** the two that print narrow intervals around positive values (which is exactly why M4's sign-based control failed). **Tail finished 83/83/83/26 with a completion sentinel** — and the new tag-aware guard, on its first round, correctly declined to relaunch on top of two orphaned samplers that M4's guard would have doubled. **ESS floor registered at 100, and it moved a headline**: M4's B-2 seam-(b) shift is **WITHDRAWN** (+0.259 → +0.040 floored; jackknife +0.257 ± 0.212 = 1.2σ — the threshold rule never carried an uncertainty), **replaced by a stronger paired result** (49 of 70 down, Wilcoxon p = 5.8×10⁻⁶, control p = 0.68). Its E5 falsifier came back **negative** — rejected runs agree *better* than admitted ones — so the floor is claimed only as a bound on our own Monte-Carlo error | **Mint the Zenodo DOI — it is the single blocking item for submission** (and it unblocks itf-linker C5 too) |
| gaia-dr4 **M9 ✓ — FRONT CLOSED, READY FOR 2 DECEMBER** | [`gaia-dr4/`](gaia-dr4/) | **The chain had never been run, and running it broke four things component testing could not reach — including that the December refit command in the runbook could not consume a single December source.** Harness → refit arm → v2 store → the pre-registered labels now runs as **one thing over the real 981-row queue in 7.8 min of compute** (adjudicate 51 s → refit 98 s → labels 319 s) on top of transport, which is **94 % of the day** (≈ 2.2 h end to end at M7's median). **Resume was tested by SIGKILLing every stage**: adjudicate 260/981 → 981, refit 56/219 → 219, labels byte-identical, **0 duplicated and 0 lost rows, no orphan `.tmp`**. The four scale-only defects, all fixed: **(1) `orbital_refit_arm.py --ids`, the command this runbook prescribed, routes to the twelve-source pre-release file** — it answers NO_DATA for every DR4 id then dies on a KeyError; a `--queue` entry point that eats the harness's own ledger now exists (**219 refits, 0 failures, 0.44 s/source, 218 of 219 zero-point-corrected**). **(2) DR4's `astrometric_params` is a bitmask, not DR3's 3/31/95 — 17 of its 19 declared values fail `zpt.get_zpt`, 11 of them the non-single-star values, i.e. the entire candidate list** — and because the house wrapper *masks* rather than raises, **the zero-point would have silently vanished on all 981 rows** and every mass shipped high by ~3Z/ϖ. *A defensive mask turns the raise you were watching for into a silent total loss.* **(3) `os.replace` killed a 981-source transport run at source 360** (Windows sharing violation) — retried now. **(4) Two harness runs on one ledger duplicated 180 rows** with nothing to stop them — `LedgerLock` now refuses the second writer. **December's zero-point comparison is a prediction, not a shrug**: L21's Z frozen for all 981 (median **−35.47 µas**, 99.8 % correctable), **five verdicts with numeric thresholds pre-registered and run 6/6** against declared synthetic bias columns, and which correction wins decided in advance (**DR4's own column wherever usable**; CONVENTION FLIP ⇒ STOP, worth a median 8.4 % and a worst 88 % of a companion mass). **a₀'s external calibration is closed by being declared impossible, with a number.** The whole published non-Gaia supply of photocentric semi-major axes — **Hipparcos DMSA/O ∪ ORB6 grade 9** — is **36 systems** after matching (**16 in both**, because most ORB6 grade-9 rows cite `HIP1997d`), its σ(a₀)/a₀ is **13.6 % against Gaia's 4.7 %** — **2.9× worse than the thing it would calibrate** — and its **measured power against M8's ×1.4 is 10 %**. It *can* bound the **scale**: **0.979 ± 0.004**, but two named alternatives of the same sign (Eddington bias on near-threshold Hipparcos-era detections, ρ = +0.24 in the predicted direction; and a band-dependent photocentre) cannot be separated at n = 36, so **the deficit is not attributed to Gaia**. A fully external one-sided route agrees — **SB9's K₁ → a₁ sin i on 53 astrometry-only solutions**, where a luminous secondary can only push the ratio down: median **R = 0.968**, over-run above 1 at **5.7/1.9/1.9 %** vs 15.9/2.3/0.14 expected. **Rehearsal green in 41 s** with every M9 change in; **13/13 frozen artifacts unchanged**; **no config v7** (nothing about the list moved); **pre-registration NOT edited, variant log still empty**. New landmines: **a z-score against one side's error is not a z-score** (Gaia BH1's a₀ is +24.8σ against EB26's error alone and **+1.6σ** with both); a rehearsal that checks the verdict and not the numbers beside it ships a **1000× unit error**; the ledger checkpointed every batch and the **instrumentation did not**, so a crash lost every timing; and a raw epoch row is a **field-of-view** transit while the INCONCLUSIVE gate is on **CCD** transits, ~8.5× more. **Runbook rewritten in seven places** with the full day-one sequence, measured timings, six new failure branches and **first-24-h / first-72-h checklists**. **M10: there isn't one — the front is ready and waiting for 2 December.** Matthew's, and only Matthew's: the four pre-registration amendments + **the GAP-4 ruling, which still decides whether December may claim a D4 null**; the two free accounts; and excluding `data\epoch_cache\` from real-time antivirus scanning |
| gaia-dr4 **M8 ✓** *(superseded by M9)* | [`gaia-dr4/`](gaia-dr4/) | **The 2.4σ closes, the inflation factor is measured on 202 real comparisons instead of 11, and the pre-registration has been *run*.** **(0)** The literal ask cannot be done by anyone: **Gaia DR3 publishes no stellar epoch astrometry**, so the refit arm's Keplerian half has twelve sources of input in the world. **(1) Inflation ×1.40 [1.31, 1.52]** against **SB9** (Pourbaix+2004 — ground-based spectroscopy, no shared photons), 202 elements from 138 systems, with the coverage test the brief asked for: **52 % inside 1σ where 68 % is expected**, 85 % inside 2σ where 95 % is. It **rises monotonically with `significance` (0.88 → 1.83)** — the axis this list selects on — falls with period, is flat in G and across solution types, and reweights to **1.19** on the queue's own population. **400 injection–recovery runs through the arm's own fitter settle what M7 could not**: the Laplace σ is **1.05 [1.03, 1.09]** with a correct noise model and **1.51** with one unit of unmodelled jitter, so the inflation is **model misspecification, not a broken Hessian** — and M7's "×2.3" was a median |z|, which on this convention is **×3.4**. Named limit: SB9 gives P and e; the mass goes as a₀³ and a₀ still has no external calibration. **(2) The zero-point is closed and half of M7's caveat was a convention mismatch**: Panuzzo corrects his single-star parallax by **35.4 µas** (reproduced here to **0.006 µas**) and explicitly does *not* correct the NSS orbital parallaxes M7 compared against. **Applying L21 moves Gaia BH3 from M₂ 34.68 to 32.64 M☉ and from +2.42σ to −0.07σ against Panuzzo's published 32.70 ± 0.82**, and the corrected refit parallax lands on his **zero-point-free** a₀/a₁ value to **+0.11σ** where the raw one sat 1.90σ away — a prediction that could have failed. Across the queue the median companion mass moves −1.95 %, but the **M₁-free mass function moves −4.1 % median and −33.7 % worst, and six of the ten top candidates have no M₁ point mass** so f_M is all they have. **Residual bounded three ways at ≤ 2 µas ⇒ ≤ 0.4 % of a mass at 1.7 mas.** Wired in as `--zeropoint` (default off so M7's frozen acceptance still reproduces byte-identically). **(3) The pre-registration executed** on 11 synthetic stores at December's projected sizes: **55 runs, 0 non-zero exits, 77 labels, every targeted expectation met** — and it found **the D4 command does not parse** (no `--scopes`; M7's "the commands run" was true only of the four M7 typed), **a December-scale crash in `m5_activity_discriminator.py`** (a *binary* metric reaches the confound guard for the first time at 633+347 and `np.clip` on a boolean Series kills both the primary and pooled runs), and **four gaps where the frozen registration determines no label** — the worst, GAP-4, fired 11 times and **costs December the D4 NULL depending on which sentence of §4 is read**. Registration NOT edited; amendments written up for Matthew. **The central promise does execute**: min-detectable AUC 0.575 vs effects of 0.659/0.656, so a non-significant D1/D2/D3 reads **NULL** (17 times). Also measured: **D1 and D2 are not independent axes** (AUC 0.873, p 7×10⁻¹²) so two positives are one finding. **(4)** Runbook rewritten in six places, rename map + **config v6** (membership still 949, acceptance re-checked), **nine-stage rehearsal all green in 56 s**, plan-B pull byte-identical for the **sixth** time. Bonus from reading the DR4 draft data model: **`tentative_parallax_bias` exists in DR4's own `gaia_source`** — ESA ships its own zero-point on L21's convention, so December prefers it. New landmines: a **frozen M7 artifact still carries the 2⁵³-rounded source id** M7 itself documented (fixing code does not fix what the bug already wrote); `iterrows()` is the same trap; two `--out-dir` leaks wrote over frozen files; and **the label `NULL` is pandas' default NA token**, so `read_csv` deletes every null from December's own label file. M9 rec: take the amendments to Matthew; cross-calibrate L21 against `tentative_parallax_bias`; get an **external reference for a₀**; run the production chain end to end at December scale |
| dyson-revet **M6 ✓** *(superseded by M7)* | [`dyson-revet/`](dyson-revet/) | **The morphology stage is built and it closes 5.1 of the last 17.8 points — not all of them, and the remainder is named.** **N4** reads the AllWISE W3/W4 coadds directly: **S = σ_obs/σ_exp** in a 12–45″ annulus, the robust dispersion of the PSF-smoothed image over what the coadd’s *own uncertainty image* predicts — one line, **no training set, parameter-free null of 1** (measured 1.396/1.245 on 27,876 |b|>50° parent stars), threshold = **M5’s N2 rule verbatim** so **no new free parameter**, measured FPR **1.20%**. **All three validations pass: 7/7 preserved** (N4 flags 0 of 10 labelled), flagged fraction falls **monotonically** 70.3→1.1% with |b|, and at |b|>50° that **1.14% sits below its own 1.20% false-positive rate** — it does nothing where M4 proved nothing needs doing. **Pre-visual survivors 1,545 → 585 (M5) → 411 vs the paper’s 368; overproduction 4.20× → 1.59× → 1.117×, 96.3% of the excess removed, every latitude band now within ±50% of the published rate.** **But at the RMSE gate ours rejects 36.3% vs their CNN’s 49.0% — 5.1 points closed, 12.7 remaining**, candidates for the residual stated not guessed. **N4 sees what N1/N2 cannot: 486 objects (5.1%) no catalogue lists and whose background is not in the top 1%.** Its one failure mode (70% false flags at W3<5, PSF wings) was found and bounded — **zero survivors are that bright**; magnitude-matched enrichment **289× in the plane, 88× all sky**. **Candidate D’s MRS cubes are REDUCED** — 12 public L3 cubes, 4.90–28.70 µm, 11,625 slices deblended, same newer `CAL_VER 2.0.1` re-reduction as the imaging — **and the first independent extraction of the contaminant’s spectrum exists. It cannot settle z ≈ 0.922, and the reason is measured**: PR-2’s acceptance test **FAILS 4-of-6 with the *dominant* component passing in every band and only the *sub-dominant* one failing** (PSF-wing leakage across 1.23″ — a direct warning for E); the blind scan’s best z moves **0.47→1.06** on a ±0.02 dex continuum change; **41.1% of the redshift grid matches z=0.922 on the narrow-line test**; the star control throws a 4.16σ spurious peak; and **sub-band stitching offsets reach 11–28%, the size of the features themselves**. **No redshift is quoted, per the pre-registration.** It *does* show the contaminant is spectroscopically non-stellar (×250 from 5→25 µm), reproduces M4’s 10–15 µm index **3.79 vs 3.8**, and tests M4’s 441 K blackbody for the first time at **394 K**. **Completeness measured — 75,600 injections**: in-grid pre-visual recovery **50.2% all-sky, 45.8% at |b|>30°**; **the RMSE fit is not the bottleneck** (90.3% pass) — 20.2% never detected, 31.2% lost to the host’s own Gaia flags. **Two hard walls quantified: γ=0.01→0.00%, 0.05→2.5%, 0.10→43.9%** (below γ≈0.05 the screen is *blind*), and **T_ds=1000 K→0.17% vs ~31% inside the grid — the grid’s temperature range is a second wall nobody had costed**. **Control: 8,400 bare photospheres → 0.00% RMSE-gate false positives.** **Catalogue v2 shipped** (same 223 rows, 13 new columns, own README; **v1 untouched**). **Candidate E READY**: outcome map **SHA-256-verified byte-identical** to the committed M5, chain reproduces all 7 checks, E still **0 PUBLIC of 39, release 2026-09-09**. **Route finding: IRSA IBE 503s in ~0.1 s under concurrency, cap ~12 req/s per client that splitting does not raise — and a fetcher reading 503 as “no image” silently marked 927 good objects invalid** (fixed, with cache repair) |

*itf-linker left to the concurrent session (it shipped M12 and a CI fix). erosita-dr2's classifier build stays behind Matthew's pivot decision.*

## Completed fronts — wave 7 (2026-08-21 → 24)

| Front | Folder | Scope |
|---|---|---|
| pta-mpta **M4 ✓** | [`pta-mpta/`](pta-mpta/) | **The array is finished: 83/83 pulsars gated, and the reproduction agrees with the MPTA published noise table on 576 of its 588 values (98.0%), 73 of 83 pulsars in full** — every DM GP, chromatic GP, chromatic Gaussian event, annual term, solar-wind GP and free red process in the release, rebuilt from public data. **All 12 misses have exactly two named causes:** 10 are the solar-wind spectral index (or the amplitude coupled to it) on the 8 pulsars whose published γ_SW is negative or crosses zero — outside the prior a reproducer must guess — and **2 are the same parameter on exactly the two rows the paper prints in bold because their values come from the CURN analysis, not the favoured model.** ΔlnL(ours − published) median **+0.70**, 79 positive / 4 negative. **The scale-relative stability gate was registered BEFORE resuming and both outcomes are reported side by side everywhere: 83/83 under it vs 76/83 under M3's absolute rule, with an identical agreement rate (98.0% vs 97.9%)**; its falsifier passes and its cost is measured (median minimum ESS 105 vs 347). **γ_SW wide variant (registered, not post-hoc): 24 of 26 SW_Full pulsars compared — including all 7 with a negative published γ_SW — and U(−4,4) resolves ALL 10 of the campaign's solar-wind misses and creates none, so after the variant the only disagreements left in the 588-value table are the two σ_g values on the two bold rows the paper itself sources from the CURN analysis.** **Its registered control FAILED — and that is a finding**: the control was keyed on the sign of the published γ_SW, which is not a proxy for "measured"; **5 of 24 solar-wind rows widen their γ_SW interval by >2× under a wider prior, one of them (J1744-1134) with a *positive* published value**, so their apparent constraint is the prior edge. **CURN: the 83-pulsar factorised likelihood gives log10 A_CURN = −14.44, 68% [−14.64, −14.35] — consistent with the published 83-pulsar −14.28 ± 0.21**, the first independent reproduction of an MPTA common-signal amplitude at array scale; M2's top-ten reproduces to 0.02 dex. **M3's "the effect is the width, not the shift" is WITHDRAWN** — at 82 common pulsars the 2.54-dex blow-up is gone (0.29 dex) and a **real +0.259 dex shift** remains, because **the FL product's interval stays pinned to the prior floor until J1909-3744 enters and then collapses from 1.92 to 0.37 dex in ONE step** — the array's strongest single constraint does it alone. **RNAAS table-audit note DRAFTED — NOT SUBMITTED** (1,391 words + one table, limits verified live, **NOT SCOOPED** across 44 citing works, no prior file anywhere in the release — verified by opening the tarballs; 29 numbers re-derived by an independent parser, 1 correction and it was in M4's own pre-registration). A paragraph Matthew could send the MPTA is **DRAFTED — NOT SENT** inside it. **Venue bar: 3 of 4 conditions MET; the only gap is a citable DOI, a human step.** M5: Zenodo DOI, finish 2 `swwide` runs, register an ESS floor, re-specify the solar-wind control, write the paper |
| gaia-dr4 **M7 ✓** | [`gaia-dr4/`](gaia-dr4/) | **The day-one clock has a measured centre for the first time, and the half of M6's band a better experiment could remove is removed: M6's two transport models were never rivals — they are the two terms of one cost model, and the probe that could not separate them was collinear by construction.** A **981-source dry run through the production harness** at a *fixed* batch 20 with payload varied **4.7×** across batches (batch time then varies 6.0× where a flat model predicts 1.0×) separates them at **13.5 σ**: `t = 2.42 s + 0.215 s/source × n + 0.1424 s/KiB × KiB` (R² 0.878) ⇒ **468 sources/hour, the 981-row queue in 2.1 h — a measured central value where M6 had none**; the band that remains (126–803/hr, 1.2–7.8 h) is a similar width to M6's but a different object — every edge is an archive state somebody measured rather than a modelling choice, and a **sustained** run varies by only ±8 % because a 50-batch clock averages single-request extremes away. M6's 78-h worst branch → **20.2 h**. Empty-request overhead **0.65 s** not 2.3–6.0; **only 7.5 % of the queue has DR3 epoch photometry** (hence a payload-stratified control set); **resume demonstrated at real scale** (stopped at 400, resumed 581); 981 sustained fits at 0.123 s, no drift, f2 bit-identical. **The orbital-refit arm is a pipeline** (`orbital_refit_arm.py`: orbit → M₁-free mass function → **companion-mass posterior**, M₁ from the triage's own ladder, output as **verdict-record v2** = v1 + 35 `refit_*` fields, v1 untouched): **acceptance PASS — Gaia BH3 re-derived to P 11.454 yr / e 0.7278 / M₂ 34.684 M☉, inside M1's printed precision on all three**; on the trio every BH3 Campbell element is within **1.1 σ** of Panuzzo's astrometric solution, **HD 114762 lands with Winn 2022 (0.233 vs 0.215 ± 0.013) and excludes Kiefer's 0.10–0.14**, and Gaia-4 gives 10.8 vs 11.8 ± 0.7 M_Jup. **Two caveats measured, not asserted: the formal error bars are lower bounds by a median factor 2.3, and all three refit parallaxes ran 5–41 µas LOW against a mass function that goes as ϖ⁻³** — which *is* the arm's 2.4 σ offset from Panuzzo's published M_BH. **December is pre-registered and frozen** (`PREREG-2026-08-23-…`): scope-pure primaries, **pooled results interpretable in one direction only**, six pre-assigned outcome labels, Holm family sizes fixed, negative control given a veto — and because one harness pass clears every threshold, **a non-significant December result on D2/D3/D4 is a NULL, not "underpowered"**. New landmines: `DataFrame.iloc[0].to_dict()` rounds `source_id` past 2⁵³ (the pandas twin of M2's ADQL one); Newton diverges on the mass-function cubic and returned M₂ = 1e−9, caught only by the cross-check against pystrometry. **No config written — M7 moved nothing about the list.** M8 rec: run the arm at queue scale against DR3 NSS to turn the ×2.3 error-inflation factor into a real distribution; bound or correct the parallax zero-point; and **rehearse the pre-registered December analysis on a synthetic store** — a pre-registration nobody has run is a promise, not a protocol |
| dyson-revet **M5 ✓** | [`dyson-revet/`](dyson-revet/) | **Vetting finished: 719 contamination-consistent / 584 indeterminate / 242 sub-threshold / 0 still-clean** — the zero is *by construction* (V5 retired), so the survivors are "no **detectable** contamination given a method with a known blind spot"; no Matthew-gated candidate. **Nebular stage built and it closes 81.6% of the gap**: N1 vetoes on the published angular extent of **29,462 nebulae from 14 VizieR catalogues (zero free parameters)**, N2 on AllWISE's own coadd background at a rule-fixed 0.99 percentile; funnel **4.20× → 1.59×**, keeps **7/7** published candidates, and the decisive check — residual falls 20.89→2.62× in the plane while |b|>50° stays **1.05→1.05×, untouched**: the stage does nothing where M4 proved nothing needed doing. **It deliberately does not close the plane, and that is the result**: ours rejects 31.2% at the RMSE gate vs their CNN's 49.0%, so **the 17.8-point gap is a published measurement of what the unpublished stage was doing** — ~1.6× more than every catalogued nebula on the sky plus a 1%-FPR background cut. Bonus: V4's chance-alignment prior measured locally for the first time (**factor 372** off a single global value) and still far short — the interlopers that matter are the ones AllWISE never resolves. **Route finding: IRSA Gator's anonymous multi-position upload runs 0.0027 s/position vs TAP's 3.5 s — ~1,300×** (a 3-hour pass in 8.1 s), gated behind a pre-registered 1,545/1,545 acceptance test. **Catalogue shipped**: `catalog/dyson-revet_highlat_extreme_IR_excess_v1.csv`, 223 objects with the 90-object calibrated core flagged, rejects kept in-table with their evidence, completeness stated *including what is UNMEASURED*. **Candidate E ready**: D's chain parameterised and reproducing all 7 checks, E confirmed same-structure and public **2026-09-09**, with a four-case outcome map — including the falsifier for the V5 retirement — written before the data open |
| itf-linker **M12 ✓** | [`itf-linker/`](itf-linker/) | **The daily archive read as a series for the first time — 26 days, 24 snapshots — and the ITF is DRAINING: 168,078 observations left against 38,319 that arrived, 4.4 : 1, net −129,759** (9,322,655 → 9,194,631). **The drain is linkage, not data loss, and that is now measured rather than inferred:** re-designation is **0.15%** of departures (the confound that would have made “draining” mean “churning” — `obs_key` folds `desig`), **99.57% of departed observations belong to designations that vanished ENTIRELY**, and a random sample of **150 departed tracklets — none of them ledger rows —** attributed against the full 1.56M-orbit MPCORB and checked against the absorbing objects’ own published records **confirms 133 (88.7%, Wilson [82.6%, 92.8%]), every one matching the tracklet’s ENTIRE observation set, 133/133 with zero partials**. The 17 misses all failed at *attribution* (best separation 62–1,698″) — and **re-running all 17 at confirm depth 50 (850 MPC queries, ≥132 candidates each) added ZERO confirmations**, so the ceiling is NOT a ranking artifact and more depth will not move it; what is left is the one thing a catalogue search structurally cannot see, an absorbing object absent from MPCORB. **88.7% is a floor on linkage and 11.3% a ceiling on “not a linkage”**, never a measurement of it. **The event only a daily archive could have caught: for five days, 08-17 → 08-21, Pan-STARRS intake collapsed 86×** — F51+F52 supply **81% of everything entering the ITF** (1,640/day) and fell to **19/day**, while removals continued at 6,600–13,700/day. **The apparent recovery on 08-22 is a different observatory**: Pan-STARRS is still at 18/day; the rebound is one V00 batch. A one-day precursor sits at 08-11. **08-24 (one day past the frozen window) makes it eight consecutive days at 0.5% of intake** — if it stays there the ITF stops growing and drains monotonically. The MPC regenerated the file every one of those days, so this is not a stalled pull — and M12 does **not** adjudicate the cause (four fit, including “identified immediately, so never reached the ITF”, which would mean the opposite). **M0’s own ground truth is eroding**: `DISCOVERY/itf-linker.md` defines it as “the **2,515** designations spanning 3+ nights” — now **2,455**; that same section asked for exactly this snapshot-diff control and said it “requires archiving snapshots starting now”, written the day the archive started. Walk verified **5/5** against every surviving key set **and** M9’s independent 08-16 reconstruction, reproduced exactly. **Seven traps paid** — a delta chain transports *distinct keys* (the first verification failed all four checks by exactly the file’s `duplicate_observations`); M9’s reconstruction drops whole *tracklets* `(desig, obscode, night)`, not designations (off by 98); an output-capping prefilter must **sort first** (1/10 → 4/10 → 8/10 at no wall-clock cost); `get-obs` rejects an ITF trksub outright; light time at 2,389 AU is 13.8 d and killed a 50/150 run that had checkpointed nothing. **Retention was the recurring enemy and is now fixed at the source**: a 3–4 day rolling key-set window cost M11 a silently wrong interval and M12 two permanent holes, so `FULL_KEEP` 3→14 (local — decides whether the NEXT delta is computable at all; the task passes no override) and `KEYSET_KEEP` 4→14 (release mirror — decides whether a pruned key set is RECOVERABLE), with a test asserting the mirror is never shallower than the window. ~2.6 GB against 703 GB free; repairs nothing, stops the fourth loss. **M11 §9’s five items are untouched and all still rank above this** — the submission decision is still Matthew’s. **521 tests** |

## Completed fronts — wave 6 (2026-08-21)

| Front | Folder | Scope |
|---|---|---|
| dyson-revet **M4** | [`dyson-revet/`](dyson-revet/) | Finish the sky **without ESAC** — the AIP mirror hosts every joined catalogue and its anonymous async works; **measure the parallax proxy's PURITY** (M3 only measured 99.09% recall, which cannot bound parent-sample contamination) before trusting it; settle the **1.43× parent discrepancy** so an absolute sky-wide yield can finally be quoted; close or bound the **Gvar reference gap** (currently explains part of the 4.77× overproduction); and **analyse candidate D's now-public JWST data** — the first chance to calibrate our archival axes against the imaging that actually settled a candidate |
| gaia-dr4 **M6** | [`gaia-dr4/`](gaia-dr4/) | **Build the verdict-harvesting harness** — three milestones of discriminator tests all hit the same wall (65 published verdicts), and the epoch-vet loop is the only machine that makes more. Production DataLink→`gaiasupdate`→f2 loop, batched/resumable/instrumented with a **measured sources-per-hour and projected day-one wall-clock**; a verdict schema where external and harness-made verdicts are the same record type; M4/M5 tests refactored to consume it and required to **reproduce their published numbers exactly**; `flag_astrom_quiet` settled or explicitly deferred |

## Completed — 2026-08-21

| Front | Folder | Scope |
|---|---|---|
| dyson-revet **M3** | [`dyson-revet/`](dyson-revet/) | **The W4 screen stalled, not finished**: it stopped 2026-08-19 03:56 at **93/192 tiles — 19,874 deg², 48.2% of sky, 220,632 W3W4 rows — with 99 tiles abandoned**, every one exhausting its retries on ESAC returning non-VOTable HTML (plus 114 outright 500s). **ESAC re-probed healthy 2026-08-21 (HTTP 200 in 1.5 s)**, so M3 resumes: `repair` the orphaned split, work the 99, then deliver the funnel at both grid floors vs the paper's rates with Poisson intervals, vet every survivor (centroid floor + the two axes M2 invented: WISE release-consistency and `w?nm` single-exposure counts), close the two standing method caveats, and check whether JWST GO 7199's candidate-A result has published. Tiles were issued in pseudo-random sky order, so the 48.2% already on disk is an **unbiased** half-sky sample even if the rest never completes |
| pta-mpta **M3** (reported, campaign still running) | [`pta-mpta/`](pta-mpta/) | **The noise table reproduces to 296/299 parameters (99.0%) on the 48 of 83 pulsars gated so far, 46/48 in full** — chromatic Gaussian events, annual chromatic terms, free-β chromatic GPs and DM GPs all rebuilt from public data. **Every one of the 3 misses is γ_SW on a pulsar whose published value is negative**, i.e. outside the prior a reproducer must guess: the paper tabulates **no priors at all**, and **7 of 26 tabulated γ_SW values cannot be reached** under the standard γ ∈ [0,7] (e_e's own solar-wind default is U(−2,1)); a declared post-hoc rerun with U(−4,4) turns J1327-0755 from 3/5 to **5/5**. **Table audit, no sampling: 26 of 588 published values (22 pulsars) have a MAP outside their own 68% interval, and 66 of 83 A_13/3 rows are prior-bounded** — only 6 constrained better than 0.7 dex. **Seam (a): the A–β ridge is universal (r = −0.90) but only 2 of 9 free-β pulsars are prior-driven** — and **M2's J1017 "prior finding" is corrected to data-driven**; bonus, re-quoting A_Chrom at its own ~855 MHz decorrelating frequency instead of 1400 halves its uncertainty for free. **Seam (b): real, one-directional, and smaller than M2 claimed** — 8 of 26 pulsars above the null control, 18 of 26 moving down, max 0.77 dex; **M2's 1.3 dex J1600 claim is withdrawn** (whites-fixed confound). **CURN: −14.30 (36 psr) / −14.18 (33 psr), both consistent with the published −14.28 ± 0.21; the model choice shifts the MAP 0.14 dex but blows the interval from [−14.63,−14.12] to [−16.85,−14.31]** — the cost of the collaboration's own mitigation, priced. **83-pulsar FL not reached.** A1 now **82/83**, with the internally complete 63 matching tempo2's TRES to 0.02%; **J1825-0319's released ephemeris carries an unphysical negative Shapiro amplitude**. **Venue: bar not yet cleared** (coverage; the CURN result is a width not a shift) — but the table audit alone is a finished RNAAS-sized product. M4: finish the array (60–100 core-hours), register a scale-relative stability gate, run γ_SW wide as a registered variant |

## Completed fronts — wave 5 (2026-08-18)

| Front | Folder | Wave-5 scope |
|---|---|---|
| itf-linker **M10** | [`itf-linker/`](itf-linker/) | **Priority 1 serves Matthew's review: refresh live-vs-consumed across all ~733 ledger rows, re-measure the decay clock, and emit `out/review-queue.csv`** (live rows only, ranked by submission value, human-adjudicable columns + a 10-row spot-check). Then the 15–25 yr main-belt shell, M9's 60 new ambiguities, and the pointed-field screen for the TNO confound |
| gaia-dr4 **M5 ✓** | [`gaia-dr4/`](gaia-dr4/) | **The activity axis, footprint penalty removed — 65 of 65 verdicts vs M4's 29 — and it still doesn't discriminate.** **`activityindex_espcs` is NOT TESTABLE (7 of 76 targets), which refutes M4's own premise that it was all-sky.** Photometric variability (Belokurov+17 amplitude, mag-detrended): **underpowered null** — AUC 0.659 [0.507–0.805], Holm 0.141, vs 0.725 detectable; **direction agrees with M4's X-ray direction** (spurious = more active) and needs ≈2× the sample (84+46). What *does* discriminate is **astrometric quality, pointing the other way**: `astrometric_gof_al` Holm 0.0067, `ruwe` Holm 0.041 — **confirmed hosts are the NOISIER fits** (the measured version of `ruwe_cut = NONE` since v1). One tiebreaker flag frozen (`flag_astrom_quiet`) with both caveats *in the config*: it retains only p=0.048 controlling for `significance`, and catches **0 of 7** in-list spurious. Negative control clean. **Southern dust CLOSED** — Vergely+2022 all-sky, sourced A₀(550) chain, **pre-registered geometry gate** (ρ 0.966 vs 0.38–0.41 corruptions): all 4 southern rows survive, V22 reproduces Bayestar19 9/9, 1 σ-fragile flagged, **0 movements → v2 list (949) stands**. Config **v4** gated by acceptance PASS. **Queue now emitted by the rehearsal driver** (stage H + shared builder that asserts BH1/BH2 itself). **Landmine: ESAC TAP down for hours** (500s/timeouts on one-row queries) while ARI/AIP DR3 mirrors answered in <2 s and matched ESAC to 0.000e+00 — retries added everywhere; **the mirrors have DR3, not DR4**. M6 rec: build the day-one **verdict-harvesting harness** — every axis is now sample-limited, and the epoch-vet loop is the only sample factory |
| dyson-revet **M2 ✓** (W4 pull still running) | [`dyson-revet/`](dyson-revet/) | **D retired** in four places, append-only; its centroid data kept as the *calibration of the method's blind spot* (a JWST-confirmed contaminant at 1.0″ moved the centroid only 0.5–1.4″ → the 1–2″ floor). **[`I-dossier.md`](dyson-revet/I-dossier.md): I is not unrefuted, it is unvettable** — both bands fall *below* WISE's own on-ecliptic 5σ limits (0.44×, 0.62×), both sit inside neighbouring sources' upper limits, W3 is quality-flag 'U', the independent reduction never detected W4, and single-exposure detection counts are 0 and 1. Excess restated in flux: **W3 1.3σ, W4 3.3σ, joint 3.5σ pre-trials** (W2 control 0.5σ; a colour-sign bug that faked an 8σ W2 excess was caught *by that control*). Nothing but WISE has ever observed it above 5 µm. Settling it = **one JWST/MIRI visit, ~1.2 h charged**. **Bonus: JWST GO 7199 has a third target matching candidate A** (observed 2026-07-14, absent from Hephaistos IV) — the JWST-vetted sample becomes 3. **Ren+24 note drafted (1,048 words, DRAFT) — but prior art exists: Blain 2024 fn 6 already flagged the slip with "(sic)"**; what's new is its magnitude and that it *inverts* the conclusion. Three verification checks were bot-blocked and are flagged must-do. **W4 route correction: M1's async-strip plan is broken (anonymous async 500s on every job — never tested in M1)**; sync has a fixed ~181 s wall independent of tile size, so retry beats splitting; 4/192 tiles done (752 deg², unbiased pseudo-random order), ~7 h projected. **New finding: the model-grid floor is worth ~9×** — at the paper's stated grid the funnel is Poisson-consistent with its published 368, but at the floor their own candidate F requires it overproduces 3.7× | **Matthew: is the Ren note still worth submitting given Blain?** and **does the I dossier become a JWST DDT proposal?** |
| erosita-dr2 **write-up ✓** | [`erosita-dr2/`](erosita-dr2/) | **Venue: one RNAAS note** (limits verified live: 1,500 words, one figure *or* table, non-peer-reviewed) — a short paper would need padding the census with per-object dossiers no follow-up supports. Draft is **1,376 words, one figure, DRAFT — NOT SUBMITTED** ([`draft-rnaas-vanished-census.md`](erosita-dr2/draft-rnaas-vanished-census.md)). **Headline: of the 261 bright eRASS1 sources absent from DR2, 57% still have flux at their position (catalogue artifacts) and 41% are real faders** — the release paper's §5.1 "unmatched ⇒ spurious" breaks at the bright end and breaks harder the brighter the source (100% artifacts above DET_LIKE 242). **Prior art: NOT SCOOPED** — nobody has ever published an eRASS1↔eRASS:3 variability/vanishing census and no DR2 variability VAC exists — **but Ramos-Ceja+2026 §5.1 already runs the same UID_DR1 cross-walk and publishes 21% / 3.5% / 0.15% unmatched while never mentioning variability**; the draft now opens on it as the thing being tested. **Audit: 107 numbers, 93 verified / 14 corrected / 1 UNSOURCED dropped** — the threshold band is **+17/−8, not M2's +35/−12**; M1's "12.6% spurious + 8.4% variability" is not in the paper; `fade_frac` never discriminated. **New measurement**: the fader threshold had only been calibrated on ≥20σ sources, so 60 steady controls matched in flux *and* DET_LIKE were queried — **disjoint from the faders (2.03–3.78 vs ≤1.49), 0/60 misclassified → <4.9% contamination**. **J0944 fenced and absent** (it is a riser). LMC null + OGLE scoping law drafted as an optional companion note |

*erosita's classifier build stays queued behind Matthew's pivot decision.*

## Completed fronts — wave 4 (2026-08-17 → 18)

| Front | Folder | Wave-4 scope |
|---|---|---|
| itf-linker **M9 ✓** | [`itf-linker/`](itf-linker/) | **First external ground truth: the MPC consumed 30 of M8's fitted candidates in two days — all 30 went to exactly the objects M8 attributed them to** (21/21 PASSes validated, 8/8 strict-gate FAILs = measured conservatism, zero disagreements). **Ledger decay clock: 3.3% of candidates consumed per 2 days — review delay now has a measured cost.** M9 adds 272 PASS (212 caveat-free) → cumulative **~733 live PASS / 695 objects**; combined-fit tier 40 objects (**33 zero-consumed = full submission value**, median arc +3,299 d); 84/88 ambiguities resolved (1 for Matthew: 2025 HO61 vs lost 2022 UE132); deep queue is a ~28% plateau; TNO scoping = clean negative with a named systematic (survey-followup debris); latent M8 queue bug found+fixed; 6 partitions measured (2 were pure bookkeeping). 467 tests green | **the ledger review is now time-sensitive** — the 33 zero-consumed combined-pass objects first; HO61 ruling |
| gaia-dr4 **M4 ✓** | [`gaia-dr4/`](gaia-dr4/) | **Discriminator verdict: UNDERPOWERED, direction consistent — no flag frozen.** 2/13 spurious vs 0/16 confirmed detected (Fisher p=0.192; half-sky footprint caps power at spurious-rate ≥0.40); the hypothetical cut was measured and **rejected** (drops 0 confirmed, 1 spurious). Config v3 = policy only (X-ray → caution tag, epoch-vet-first); acceptance re-run PASS, EB26 operating point identical. Dust dozen closed with a paper-exact Bayestar chain (9 survive, 4 southern out of reach; Argonaut API found dead — local h5 + healpy-free reader). **Day-one epoch-vet queue built: 981 rows**, BH1/BH2 ranks 1–2, the spurious poster child rank 3 (first kill). M5 rec: the activity axis via DR3's own `activityindex_espcs` (no footprint penalty) + Vergely/Lallement southern dust |
| pta-mpta **M2 ✓** | [`pta-mpta/`](pta-mpta/) | **J1909-3744 CONVERGED 9/9 — and both BLIND chains found the mode unaided** (informed start only confirms it stays and mixes); M1's ΔlnL=+22.4 shortfall closed by sampler machinery alone, likelihood digit-identical. **Top-10 campaign: 10/10 gated, 9/10 full agreement, 76/78 parameters** — free-β chromatic GPs, two chromatic Gaussian events and an annual chromatic term all reproduce from public data. Sole miss (J1017's chromatic A–γ) is **M1's case mirrored: our likelihood prefers OUR point by ΔlnL=+4.8** → prior/convention finding on a flat A–β ridge, not a sampling failure. **10-pulsar factorised-likelihood CURN: log10 A_CURN = −14.46 MAP / −14.53 [−14.92,−14.31], consistent with the published 83-psr −14.28±0.21** (no detection claim: γ fixed 13/3, no HD/CW). **Near-miss caught: a frozen chain (acc 0.016) PASSED the pre-registered gate** — not moving is maximally "stable" — and would have published −14.95, flagged inconsistent; all 23 runs audited, gate now carries an acceptance floor. **Three defects found & fixed: an enterprise 3.5.0 bug** (varying-basis params zero the GP prior matrix → guaranteed crash sampling a chromatic index; upstream report is Matthew's call), e_e jump proposals broken under numpy 2, WSL killing detached campaigns. A1 now 10/10 (tempo2-`T2` binary + inert `TRACK -2` cleared). **Economics: M1's eval was 4.5× pessimistic** → all-83 noise campaign is an overnight job, full-array CURN 2.5–5 d. **M3 rec: cross-PTA noise criticism → all-83 → 83-psr FL → full-PTA CURN** |
| dyson-revet **M1 ✓** (new front, avenue #7) | [`dyson-revet/`](dyson-revet/) | Selection reproduced **7/7** (CNN/visual stages documented as irreproducible; Table-5 column swap found). **Premise correction: candidate D was killed by JWST 2026-07-10** (Hephaistos IV — z≈0.9 galaxy at 1″, below archival centroid resolution). **I is the last candidate standing: INDETERMINATE** (2σ excess, coadd centroids flip, no contaminant to Legacy/UKIDSS depth). Control C reproduces the published refutation to 0.05″. **Found a 3600× unit error in Ren+24's hot-DOG density** (arcmin² not arcsec²): hot DOGs explain ~0.4 of 7 candidates — the real contaminant class is faint red galaxies (~60 expected in 5M). SPHEREx axis opened (both stars photospheric to 5 µm) but the 100–200 K band needs ≥10 µm — JWST decides | review the I dossier; the unit-error correction is note-sized if you want it written up |

*eROSITA M5 (classifier thin slice) stays queued behind Matthew's pivot acceptance.*

## Completed fronts — wave 3 (2026-08-16 → 17)

| Front | Folder | Wave-3 scope |
|---|---|---|
| itf-linker **M8 ✓** | [`itf-linker/`](itf-linker/) | **482 PASS attributions across 450 distinct objects** (`m8-ledger.json`; 394 caveat-free, median joint RMS 0.082″, 90% beyond the old 4-yr wall, deepest −14.87 yr; **29 objects with 2+ independently-passing tracklets** — the PD152 corroboration pattern). Perturbed backend measured to 0.1–94″ at 15 yr (two-body was 491–7,545″); 44,192 batch objects matched 96.5% via bulk MPCORB; swept 22,636 orbits × 2.05M tracklets in 8.6 min. Decoy control at scale: sub-30″ head shows a **~560-candidate real excess**; fit queue covered only the top 0.75% and was still passing 43% at the cap — **the vein is not exhausted**. MQ241 re-derived to the digit; only 2 already-linked. Watcher built (not scheduled) and already flags **four unconsumed Rubin partitions** incl. Aug 10 (100 MB). 467 tests green | **Matthew: the submission question is now a batch process, not one object** — review protocol for the 394 caveat-free set (spot-check → tranche submissions?), plus PD152/MQ241 unchanged |
| gaia-dr4 **M3 ✓** | [`gaia-dr4/`](gaia-dr4/) | corr_vec Pr(III) validated vs S23 (σ ratio 2.27×→1.03; Pr≥99.9 % core 147→293, 0 dissolve; Pr is a ranking tier, not a cut — negative result); dust tier moves 8 out / 6 in → **v2 list = 949** (config v2, v1 frozen); dress rehearsal green end-to-end + `DR4-DAY-RUNBOOK.md`; **eROSITA join: 30 coronal counterparts vs 1.4 chance, 0 accretors** (the X-ray-loudest match is the EB26-refuted alias spurious — X-ray = activity/spurious-risk tag, not compact evidence) |
| erosita-dr2 **M4 ✓** | [`erosita-dr2/`](erosita-dr2/) | **Be-fade story does not hold: 0/25 LMC faders match any Be/variable roster** (vs 0.19 chance; 24/25 have no Be-donor-capable star) — two M2 Be/XRB readings withdrawn; demographics now 9+4 AGN-colored, 3 foreground active, 1 blue near-candidate (halo hot star equally likely, spectroscopy decides). **Measured scoping law: OGLE-IV was dark 2020-03→2022-08 — the entire fade window — future eRASS-window optical work uses ASAS-SN.** Rebase kill-checks: **PIVOT** — 49–54% of DR2 sources not trivially classified by one catalog (2.5× the kill bar) but the gap is *coverage*, not disagreement; nobody has ingested DR2 anywhere — a dossier layer would be DR2's only queryable interface. Build call stays with Matthew |
| pta-mpta **M1 ✓** (new front, avenue #2) | [`pta-mpta/`](pta-mpta/) | **Access PASSED**: 4.5-yr release fully public at Data Central (DOI 10.57891/j0vh-5g31) — 83 par+tim (245,907 sub-banded ToAs) + 10,014 epoch archives + portraits; **no chains ship at all** (the 423 MB "anisotropy supplement" is 9 MP4s) → chain-level lanes MPTA-dead, likelihood lanes open. Stack built in WSL **without sudo** (enterprise --no-deps + loud-failure sksparse shim; WSL vdisk was 100% full → venv on /mnt/c). A1: PINT wRMS = tempo2's in-release TRES to +0.35%/+0.51%. Pre-registered slice: **J2241-5236 5/5 noise params agree** (CI edges to 0.01-0.03); J1909-3744 5/9 — whites + A_13/3 dead-on (-14.288 vs -14.28), chromatic DM↔SW block stuck in a local mode and **our own likelihood prefers the published solution by ΔlnL=+22** → sampling shortfall, not model error (host ran a game at 100% CPU; both chains land the pre-registered A3 feasibility verdict). Economics: fixed-white eval 1.4 ms (64×) → full-83 CURN ≈ 1.3 d/1M iters, no sksparse needed; all-83 noise pass ≈ 1 day at 16-way parallel. **M2 rec: converge J1909 → top-10 scale → factorised-likelihood CURN (-14.28±0.21); W3 = cross-PTA noise criticism first, CURN campaign second, CW/HD deferred to the sparse-stack upgrade** |

## Completed fronts — waves 1–2 (2026-08-14 → 16)

| Front | Avenue | Folder | State | Next human gate |
|---|---|---|---|---|
| **exosat-rv M32: eta Tel B RNAAS note** | writeup | [`exosat-rv`](https://github.com/mepotts/exosat-rv) | **PREPARED, NOT SUBMITTED** (`docs/paper/rnaas-etatel-draft.md`, ~1,050 words, one table, numbers regenerate from `scripts/m32_etatel_numbers.py`). Limit re-verified: msini 0.51-1.27 M_Jup, P 20-300 d, FAP <= 0.85%. Sourcing it corrected four repo numbers: eta Tel B mass attribution (Lazzoni **2020**, and DISPUTED 47 vs Chai+2024's 29), **K 13.2 -> K_s 11.6** (1.6 mag), parallax 21.11 -> 20.6028, separation confirmed. Result got stronger: orbit is near edge-on (i=79-82 deg) so limits are true-mass to 2%. **Spillover: Lazzoni's magnitude column is wrong in 2 of 3 checked cases and it is the contrast-wall's x-axis for all 31 companions** - wall note item 0, recommend standing on the resolution gate alone | **Matthew's: whether to submit at all** (vs folding into the method paper), author/affiliation/ORCID, AAS account. Plus a targeted ADS check that no prior eta Tel B RV exists |
| exosat-rv M31: HiRISE extraction of HIP 65426 | [#1](DISCOVERY/run3-prospectus.md) | [`exosat-rv`](https://github.com/mepotts/exosat-rv) | **M31 done** (`M31-RESULTS.md`, commit e244cf9) — 27/27 frames extracted through the fibre chain, all three nights PASS contents gates; **night 2 on-sky proven at 11.8σ** (benchmark 9.8σ), nights 1/3 CCFs 5σ/2.9σ at 0 km/s with strong controls (per-night numbers are the honest ones). **Key finding: HIP 65426 b sits 40–130× below the fibre background — planetary-CCF ceiling ~2–3σ — so this corpus is a telluric/sky reference + methods asset, not a companion dataset.** Disk: stayed on ext4 (ESO filenames contain colons; NTFS rejects them), sanctioned cleanup only, science raw + masters kept. Two new traps ledgered | exomoon lever for HIP 65426 b remains the K2192 slit series — Matthew's priority call (M20 §5); M32 scope = coordinate with β Pic thread (injection gate on util_ chain first) |
| eROSITA DR2 candidate vetting | [#5](DISCOVERY/run3-prospectus.md) | [`erosita-dr2/`](erosita-dr2/) | **M3 done** ([`J0944-decision-package.md`](erosita-dr2/J0944-decision-package.md), [`M3-state-bounds.md`](erosita-dr2/M3-state-bounds.md)) — J0944 class revised by sub-band spectrum: peaks 1–2 keV, soft-suppressed at ~Galactic N_H → **Galactic VFXT/subluminous-LMXB or magnetic CV ≳2 kpc**; supersoft/TDE-thermal/Be-HMXB/blazar excluded; assumption-free rise floor **≥×20**; every catalog-testable riser artifact mode excluded (only single-visit detector artifact untestable without event data). DRAFT Swift ToO ready (5 ks XRT+UVOT), NOT sent. **State bounds: zero post-eRASS pointed X-ray data on all five shortlist objects — three never observed by Swift/XMM/Chandra at all**; J0503 = Swift-persistent through 2013, gone by 2020–21. M2 history: 381 verdicts, 1 unexplained survivor (×57 hard-spectrum riser, DET_LIKE_3=197, NWAY p_any≈0 in GDR3+CW2020, zero archival X-ray across 6 catalogs, TNS/SIMBAD/literature empty). Vanished list split 148 artifact / 107 plausible faders / 6 indeterminate (UL-server-calibrated — geometry alone would have mislabeled 94 real faders); M1's top-20 vanished was artifact-selected, corrected; M1's "G=20.6 galaxy" TDE host downgraded (g=23.5 at 7″, p_any=0.002). Several M1 headliners = known transients (ATel/EP/MAXI). Consortium file defect found: `eRASSc3_Main_GDR3` ships `GDR3_source_id` twice | **Matthew: J0944 decision** — approve M3 (Swift archival state bounds + decision package, still nothing sent)? Optional: report the duplicated-column defect to MPE (email = human-gated) |
| Gaia DR4 day-one prep | [#4](DISCOVERY/run3-prospectus.md) | [`gaia-dr4/`](gaia-dr4/) | **M2 done** ([`M2-amrf-triage.md`](gaia-dr4/M2-amrf-triage.md)) — **acceptance PASS, no cut loosened: BH1 class III at 3.38× margin; BH2 recovered via the pre-registered evolved-primary bracket** (Shahaf+23's own recipe loses it twice: P<1000 d and IsocLum). Implementation digit-exact vs S23 (𝒜 ratio 1.0000 on their 101k sample; 177/177 recovered). Calibrated vs El-Badry 2026: **92.9% completeness / 30.4% spurious-pass**; their sig>20 screen would reject BH1 itself. DR3 yield 951 class-III — top-2 by M₂_min are BH1, BH2; #3 is a known spurious → epoch-vet loop is the killer, and its prototype PASSES (keeps the 3 orbit sources, demotes all 9 quiet). **DR4 config frozen** (`queries/dr4-triage-config.json`). Correction: M1's "source_ids unstable DR3→DR4" is REFUTED; real landmines: source_id is a key in NEITHER nss table, ADQL double-rounding past 2^53, async queue can hang >100 min (ranged sync fallback proven). Next: M3 corr_vec probabilities + dust tier + Dec-2 dress rehearsal + ×eROSITA join | create Gaia Archive + NOIRLab Data Lab accounts (needs Matthew's email) |

| ITF ↔ Rubin-batch attribution | [#3](DISCOVERY/run3-prospectus.md) | [`itf-linker/`](itf-linker/) | **M7 done** (`M7-RESULTS.md`; attribution capability built — didn't exist; 447 tests green). Headwind confirmed in the primary source: MPC's Rubin pipeline auto-sweeps the ITF at designation time, so the residual pool is small by construction. Despite that: **1 PASS — 2025 PD152 attributed to two independent F51 tracklets of 2022-10-02** (combined fit 33/33 obs used, RMS 0.09″, both tracklets independently land on the same orbit, SkyBoT clear, not in the published record; arc 30 d → ~1,060 d, one → two oppositions) **+ 1 borderline (2025 MQ241, 0.0007″ over the strict ceiling)**. Method lesson: the published MPC rule is nearly vacuous for attribution — the discriminating test is whether Find_Orb actually *used* the tracklet (114/150 silently excluded it). Decoy control kept the coarse stage honest (914 real vs 944 decoy = chance-dominated). Northern-TNO stat: 5,435 slow tracklets. Next: M8 perturbed-ephemeris backend to open the >4-yr window + full-batch scale-up | **Matthew: submit the 2025 PD152 identification?** (submission JSON shape in M7 §8; MPC account + WAMO tracking is the human step) and rule on the MQ241 borderline |

## Historical queued fronts (recorded 2026-08-24; not current)

| Front | Avenue | Where it will live | Blocker / trigger |
|---|---|---|---|
| exosat-rv next milestone | #1 | `exosat-rv/` | **deliberately idle from this session** — concurrent thread owns the folder (M27/M32 active; milestone numbers must not collide); next dated triggers: β Pic header probe 2026-09-25, CD-35 joint-fit campaign from 2026-12-19 |
| NEOCP high-e watcher (4I race) | #3 | `itf-linker/` or sibling | M8's watcher design lands first |
| Dyson re-vet / hypothesis-led SPHEREx follow-up | #7, #8 | `dyson-revet/`, `spherex-pilot/` | historical row: re-vet executed; broad SPHEREx scan killed and exact six-row coverage test remains privacy-gated |

## Historical human-gate snapshot (recorded 2026-08-24; superseded above)

1. **The attribution ledger** — M8 turned one candidate into **482 PASS / 450 objects** (394
   caveat-free). Decide the review protocol: spot-check a sample, then approve tranche
   submissions via the identifications API (MPC processes 500–1,000/day), or review
   per-object. PD152 remains the flagship exemplar; MQ241 borderline unchanged. Needs your
   MPC account either way.
2. **J0944** — send the drafted Swift ToO? (`erosita-dr2/J0944-decision-package.md`; needs a Swift ToO account.)
3. **eROSITA fader-census RNAAS** — submit or not? (`erosita-dr2/draft-rnaas-vanished-census.md`,
   DRAFT — NOT SUBMITTED.) Needs: author/affiliation ("Independent Researcher" is accepted) +
   ORCID; a **citable Zenodo DOI** for the census products, scoped to exclude the fenced J0944
   material (same open item as itf-linker C5); and one manual ADS lookup to confirm the DR2
   paper's journal reference (A&A 712, A171 comes from the consortium list only). The optional
   companion note on the LMC null additionally needs the XROM team contacted — not done.
   Full list in `erosita-dr2/M5-writeup.md` §6.
4. **eta Tel B RNAAS** — submit or fold into the method paper? (concurrent thread's M32; author/affiliation/ORCID.)
5. **Accounts before Dec 2** — Gaia Archive + NOIRLab Data Lab (both free). *Nothing in the
   DR4-day runbook needs them — every path is anonymous — but the async pull branch gets a
   longer rope with one.*
5b. **gaia-dr4: the four pre-registration amendments, and the GAP-4 ruling above all.**
   (`gaia-dr4/M8-inflation-zeropoint-rehearsal.md` §3c. The front is otherwise **CLOSED and
   waiting for 2 December** — see M9.) `PREREG-2026-08-23-december-discriminators.md` is
   **frozen and has not been edited**; its variant log is still empty. Only you may amend
   it, and amendments go in that log and nowhere else. **GAP-4 changes an outcome, not just
   a label**: under §4's literal reading D4 comes back `UNDERPOWERED` where the
   difference-based reading gives **`NULL`** — 11 occurrences, unanimous, and reproduced
   again by M9's full-chain run at December scale. **Until you rule, December reports both
   readings.** The other three are label bookkeeping (a seventh label for "significant, right
   direction, not decisive"; naming *pooled: uninterpretable* in §5 so §5 and §2.2 agree;
   whether a pooled reversal is reportable), plus one interpretation note — D1 and D2 are
   correlated axes at AUC 0.873, so two positives are **one finding reported twice**.
5c. **gaia-dr4, operational rather than a decision**: exclude `gaia-dr4\data\epoch_cache\`
   from real-time antivirus scanning before 2 December. A Windows sharing violation on
   `os.replace` killed a 981-source transport run at source 360; M9 added a retry, which
   makes it survivable rather than free.
6. Optional — email MPE about the `eRASSc3_Main_GDR3` duplicated-column defect.
7. **pta-mpta — the front is closed at M6 and THREE documents wait on you. Only one thing blocks
   the paper.**
   (a) **Mint the Zenodo DOI.** It is the *single* blocking item for
   `pta-mpta/draft-paper-mpta-noise-reproduction.md` and, once pasted over the placeholder in its
   §11, nothing else in the text changes. Everything the section promises is on disk and
   deterministic. Same open item as itf-linker C5 and the eROSITA note.
   (b) **Three drafts, all DRAFT — NOT SUBMITTED**, each with its own numbers script and its own
   text-vs-artifact checker, all at zero failures: the **full paper** (137 numbers / 119 checks),
   the **table-audit RNAAS note** (`draft-rnaas-mpta-table-audit.md`, 29 / 22 — now **1,451 of
   1,500 words**, so about forty words of headroom remain), and a **new composition-jackknife
   methods note** (`draft-rnaas-composition-jackknife.md`, 1,300 words, 42 / 49). All three need
   author/affiliation/ORCID; the two notes need an AAS account.
   (c) **A go/no-go on the methods note.** A prior-art re-sweep on 2026-08-24 found that
   **Larsen et al. 2025 already published the qualitative form of its claim**; the note now leads
   with that credit and states plainly that if what remains is judged too thin, it should not run.
   That judgement is yours.
   (d) **The γ_SW paragraph for the MPTA collaboration** is still **DRAFTED — NOT SENT**, with no
   addressee — but it was **rewritten** in M6: the same sweep found `gamma_sw = Uniform(-4, 4)` in
   the first author's own public repository, so the letter now says we later found the range there
   rather than implying it was never written down anywhere. Send or not?
   (e) **After any publication**, SARAO asks for bibliographic details by email. Outward send, yours.

8. **dyson-revet — should the candidate-D negative be written up?** M7 produced an
   independent, **acceptance-validated** extraction of the contaminant's MRS spectrum
   (6 of 6 on the pre-registered test, deblend excluded as the cause by an injection
   test) that **still cannot confirm the published z ≈ 0.922**. It is a negative about a
   published claim, so under M5 PR-4's own rule it is gated before it leaves the
   repository. Nothing is drafted; this is a go/no-go on drafting.
   The front's other two calls are unchanged: the **Ren+24 unit-error note**
   (`dyson-revet/note-ren24-unit-error-DRAFT.md`, DRAFT — worth submitting given Blain
   2024's prior "(sic)"? three bot-blocked browser checks first) and the **candidate-I
   dossier** (`dyson-revet/I-dossier.md` — JWST DDT/small-GO, RNAAS note, or internal?
   settling I is one MIRI visit, ~1.2 h charged).
9. Optional — report the **enterprise 3.5.0 bug** upstream (pta-mpta M2: varying-basis
   parameters zero the GP prior matrix, so sampling a chromatic index crashes deterministically;
   reproducer + fix are in `pta-mpta/M2-converge-scale.md`). Outward-facing, so it's your call.

## Log

- **2026-08-24 (late)** — **dyson-revet M7 ✓ — the front is CLOSED pending its dated
  triggers**, and it closes on a result that is stronger for being negative. M6's named
  fix worked: an empirical PSF built from the cubes already on disk takes candidate D's
  acceptance test from **4 of 6 to 6 of 6**, graded by **byte-identical M6 code** with
  the tolerance untouched — and the redshift *still* cannot be confirmed, so **the
  deblend is no longer a candidate explanation** and the published z ≈ 0.922 stands as
  something **public data cannot independently check**. The blind tests are decisive
  because their **controls out-score the signal**. Two corrections to ourselves: M6's
  redshift criterion was **structurally undecidable** (narrow lines satisfy its
  agreement clause vacuously; broad ones cannot satisfy it), and M6's **1000 K
  temperature wall is withdrawn** — 100,800 injections in two new SED families move the
  completeness by up to **17.6 points** and show every family has its own wall.
  Catalogue **v3** shipped saying plainly that *there is no single completeness number*.
  Candidate **E is READY and untouched** (outcome-map hash unchanged across two
  milestones, 7/7, 0 PUBLIC of 39). The front now has a stand-alone hand-over document,
  [`dyson-revet/FRONT-SUMMARY.md`](dyson-revet/FRONT-SUMMARY.md). **No commits, no
  pushes, nothing sent** — one new human gate added below (gate 8).
- **2026-08-17** — **Wave 3 complete, all four fronts ✓** (two agents survived a session-limit
  kill via checkpoints). itf-linker M8: 482 gated attributions / 450 objects, the pre-2023 ITF
  opened by the perturbed backend, four fresh Rubin partitions already flagged by the new
  watcher — the submission gate is now a batch-review protocol. gaia-dr4 M3: DR4-day machine
  rehearsed green in 40 min; X-ray-as-spurious-tag discovered. erosita-dr2 M4: Be-fade null
  with a measured 886-day OGLE blind spot; classifier PIVOT recommendation. pta-mpta M1: from
  zero to reproducing MPTA's published noise numbers; full-array CURN measured at ~1.3 days.
  Next wave options queued in each M-doc; the human gates above are now the bottleneck.
- **2026-08-16** — **Wave 2 complete, all three fronts ✓.** eROSITA M3: J0944 revised to a
  Galactic-transient candidate surviving its full artifact audit; zero post-eRASS pointed X-ray
  data exists on any shortlist object; draft Swift ToO ready. itf-linker M7: attribution built
  from nothing (447 tests), **2025 PD152 identification candidate prepared** (two independent F51
  tracklets, arc 30 d → ~1,060 d) + 1 borderline. gaia-dr4 M2: AMRF triage frozen, BH1+BH2
  acceptance PASS, El-Badry-calibrated, epoch-vet prototype green; M1's source_id-instability
  landmine refuted, two real key-fan-out landmines found instead. Open gates consolidated below.
- **2026-08-15** — Matthew: "continue with the recommended next steps." Wave 2 launched: eROSITA M3
  (J0944 package + state bounds), gaia-dr4 M2 (AMRF triage), and the ITF↔Rubin attribution front
  promoted from queued to active. exosat-rv deliberately left to the concurrent thread + its dated
  triggers. Branch pushed through f928a91 with Matthew's approval (J0944 now has a public
  timestamped record).
- **2026-08-14 (late)** — **erosita-dr2 M2 ✓** (one API-error crash, resumed with context intact):
  381 verdicts, one survivor — 3eRASS J094452.8−711152, the only GENUINELY-UNEXPLAINED object.
  The vetting corrected M1 twice (top-20 vanished was artifact-selected; TDE host association
  collapsed) — the M1→M2 correction chain is the method working. M31: nights 1–2 banked
  (night 1 on-sky CCF 16.4σ at 0 km/s), night 3 reducing.
- **2026-08-14 (night)** — Matthew: **M31 approved**, M1-candidate review delegated, and a disk
  correction — **C: has >300 GB free**; the volume M30 saw at 7.2 GB is the ~1 TB data volume
  holding `~/cr2res` (the concurrent thread's 17 GB of products live there). Two agents launched:
  exosat-rv M31 (HiRISE extraction, disk-reconciliation first, no cross-thread deletions) and
  erosita-dr2 M2 (per-candidate verdicts; nothing gets reported externally without Matthew).
- **2026-08-14 (evening)** — All three fronts reported and committed. **gaia-dr4 M1 ✓**: every
  pre-release claim confirmed; BH3 orbit reproduced with ESA's own package; DataLink-only epoch
  astrometry + unstable source_ids now on the map. **erosita-dr2 M1 ✓**: 632,668 clean DR1↔DR2
  pairs; known transients recovered end-to-end; unidentified candidates delivered (headliner:
  3eRASS J094452.8−711152, ×57 rise, no counterpart). **exosat-rv M30 ✓** with the day's biggest
  lesson: run-3 avenue #1's dataset claims were all already ledgered and its counts wrong —
  corrected in place; the verify-first gate cost hours instead of wasted campaigns. A concurrent
  session opened M27 mid-M30 (first validated HiRISE extraction); both threads merged their ledger
  edits additively. Open human gates: M31 approval, m1_candidates review, two accounts, disk
  cleanup (7.2 GB free).
- **2026-08-14** — Run-3 sweep folded in ([`DISCOVERY/run3-prospectus.md`](DISCOVERY/run3-prospectus.md)).
  Fronts #1/#5/#4 selected to start (Matthew's call). Folders `erosita-dr2/` and `gaia-dr4/`
  created; three agents launched in parallel. exosat-rv agent instructed to reconcile the
  sweep's TAP findings against `docs/target-queue.md` first — the queued `cd35d1` fix may
  already be the CD-35 2722 Oct-2024 data the sweep reported as new.

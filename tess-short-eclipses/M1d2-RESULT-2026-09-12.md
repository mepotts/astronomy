# Offline catalogue parity established; localization still unvalidated

**OFFLINE_CATALOG_PARITY_PASS.** The separately frozen M1d2 run at commit
`f2be7a3` decoded the retained first-field ARI response and compared it with the
retained ESA response. All **1,290 unique int64 source IDs**, all masks and unit
scales agree; every compared unmasked numeric value agrees exactly (maximum
absolute difference zero in all seven numeric columns). No tolerance was relaxed.

Mask counts preserved: 113 each in pmra, pmdec and RUWE, two in G magnitude, zero
in RA, declination and reference epoch. Missing values were not replaced by zero
or dropped. The unchanged M1d struct and NumPy numeric routes agree. ESA's existing
BINARY2 reader is unchanged; only ARI's documented table-free service descriptor
is validated and removed in memory before the existing BINARY decoder. The
scientific table subtree and encoded stream are unchanged. No service URL was
followed; **zero network requests and no pixel recalculation** occurred.

[Complete result](out/m1d2-result.json), [manifest](out/m1d2-manifest.json),
[runtime receipt](out/m1d2-runtime.json), [protocol](M1d2-PROTOCOL-2026-09-12.md)
and [independent review](M1d2-REVIEW.md) retain the exact provenance. The manifest
hash is `5a90493fc17e08200c79bb7e12ee9c1c7676bd859f8b66e0e583d7969e1efab0`.
The source was reviewed with 15 new adapter tests, on top of 39 M1d tests;
pinned Ruff 0.16.5 passes with no new exceptions.

The parent ran a complete read-only replay, repeating both numeric decoders,
input/dependency hashes and full comparison, and obtained exact result equality.
Timing/memory are separate acceptance receipts, not equality-tested scientific
values. The existing owner-context 60-second process-tree limit wrapped both runs.

```powershell
dyson-revet/.venv/Scripts/python.exe -B tess-short-eclipses/scripts/m1d2.py replay
```

M1c's Windows null-sentinel failure and M1d's too-restrictive resource-profile
failure remain preserved. Those were software/format stops, not failed numerical
catalogue comparisons. This outcome is compatibility on one known field, not
independent astrometry or proof that either missing-field response will arrive.

Next authorized development is a separately frozen M1e attempt for the two
missing published-control fields, using identical queries and the now-validated
decoder. No unknown fluxes, PRF fitting or discovery claims are justified by
catalogue parity. Source confusion, calibrated localization, empirical negatives,
absolute background/depth validity and novelty remain separate requirements.
